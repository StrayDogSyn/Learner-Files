import React from 'react';
import { Dashboard } from './Dashboard';

/**
 * DashboardPage Component
 * Dashboard page wrapper that uses the existing Dashboard component
 */
const DashboardPage: React.FC = () => {
  return <Dashboard />;
};

export default Dash