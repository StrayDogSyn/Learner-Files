import React from 'react';
import { Home } from './Home';

/**
 * HomePage Component
 * Main landing page wrapper that uses the existing Home component
 */
const HomePage: React.FC = () => {
  return <Home />;
};

export default HomePage;